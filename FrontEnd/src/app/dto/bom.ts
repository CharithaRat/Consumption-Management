export class Bom {
  constructor(public bomId: any, public bomCode: string, public bomDes: string, public bomType: string,
              public bomUnit: string, public bomDate: string, public bomValue: any, public bomGurPeriod: string) {
  }
}
