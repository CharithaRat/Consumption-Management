export class Supplier {
  constructor(public supId: any, public supName: string, public supJoinDate: string, public supContact: string,
              public supAddress1: string, public supAddress2: string, public supAddress3: string) {
  }
}
